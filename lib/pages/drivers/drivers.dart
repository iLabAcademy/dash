import 'package:flutter/material.dart';
import 'package:huran/widget/custom_text.dart';

class DriverPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: CustomText(
        text: "Driver",
      ),
    );
  }
}
